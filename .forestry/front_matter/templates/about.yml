---
label: about
hide_body: true
fields:
- name: hero_image
  type: file
  config:
    maxSize: 64
  label: Hero image
  default: "/uploads/2021/07/26/about-waf-waf.png"
- name: big_title
  type: text
  config:
    required: true
  label: Big title
  default: Naše pobočky
- name: stores
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: name
    type: text
    config:
      required: true
    label: name
  config:
    min: 
    max: 
    labelField: 
  label: Stores
